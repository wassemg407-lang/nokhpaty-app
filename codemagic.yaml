workflows:
  nokhpaty-ios:
    name: Nokhpaty iOS Build
    max_build_duration: 60

    environment:
      node: 18

    scripts:
      - name: Install dependencies
        script: npm install

      - name: Ensure Capacitor installed
        script: |
          npm install @capacitor/core @capacitor/cli @capacitor/ios

      - name: Fix permissions
        script: chmod -R +x node_modules || true

      - name: Capacitor Sync SAFE (no npx)
        script: |
          node node_modules/@capacitor/cli/bin/capacitor.js copy ios || true
          node node_modules/@capacitor/cli/bin/capacitor.js sync ios || true

      - name: Install CocoaPods
        script: |
          cd ios/App
          pod install || true

      - name: Build iOS App
        script: |
          cd ios/App
          xcodebuild \
            -workspace App.xcworkspace \
            -scheme App \
            -configuration Release \
            -archivePath build/App.xcarchive \
            archive

    artifacts:
      - ios/App/build/**
      - ios/**/*.xcarchive

    publishing:
      email:
        recipients:
          - wassemg407@gmail.com